# 원본 사전(xls·XML, 2GB+)과 빌드 산출물 DB는 build_dict.py로 재생성한다
dict/
/NIADic.xlsx
*.db
# 원작 참고용 스냅샷
kordle.html
# 프로젝트 아카이브(540MB) — git add -A 로 딸려 들어가면 되돌리기 어렵다
/n-kordle.zip

node_modules
/.svelte-kit
/build
.env
.env.*
.dev.vars
.wrangler/
vite.config.ts.timestamp-*
__pycache__/

# Claude Code 워크트리
.claude/
# Playwright MCP 산출물(페이지 스냅샷·콘솔 로그)
.playwright-mcp/
d1/*.sql
