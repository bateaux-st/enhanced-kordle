services:
  kordle:
    build: .
    image: n-kordle
    restart: unless-stopped
    ports:
      - "0.0.0.0:3000:3000"
    # KORDLE_SECRET — .env.example 참고. 바꾸면 진행 중 게임 토큰이 전부 무효가 된다.
    env_file: .env
    volumes:
      - ./kordle.db:/data/kordle.db:ro
